WITH ID_AND_MONTH AS(SELECT ID,EXTRACT(MONTH FROM TIME) AS MONTH
                    FROM FISH_INFO)
SELECT COUNT(*) AS FISH_COUNT, MONTH                    
FROM ID_AND_MONTH
GROUP BY MONTH
ORDER BY MONTH;